import Vue from 'vue'
import { Button, Icon, NavBar, Field, Cell, CountDown, Toast } from 'vant'

Vue.use(Button)
  .use(Icon)
  .use(NavBar)
  .use(Field)
  .use(Cell)
  .use(CountDown)
  .use(Toast)
